1. Add your crt/key named after your domain name, such as example.com.crt, example.com.key
2. On host side, a tailscale instance needs to be running and connected to your server

I am not using reverse proxy because I want to use the same domain name for both headscale server and the derper.

